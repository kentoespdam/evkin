// Constants
export const PRESERVE_SCROLL = { preserveScroll: true };
export const PRESERVE_STATE = { preserveState: true };
export const REPLACE = { replace: true };
export const IS_CLIENT = typeof window !== "undefined";
