import { memo } from "react";

const OptionalField = memo(() => (<span className="text-xs text-muted-foreground font-normal">(Opsional)</span>)
)
OptionalField.displayName = "OptionalField";

export default OptionalField;