services:
  app:
    build:
      context: ../../
      dockerfile: docker/franken/Dockerfile
    container_name: evkin-app-franken
    entrypoint: php artisan octane:frankenphp --workers=1 --max-requests=1
    ports:
      - "8083:8000"
    env_file:
      - .env.staging